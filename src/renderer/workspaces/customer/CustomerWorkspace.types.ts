export type CustomerFormState = {
  name: string
  phone: string
  email: string
  address: string
  notes: string
}

export type VehicleFormState = {
  plateNumber: string
  brand: string
  model: string
  year: string
  vin: string
  color: string
  notes: string
}
